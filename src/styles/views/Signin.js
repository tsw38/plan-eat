import styled from 'styled-components';

import {layout6xl} from 'styles/sizing';

export const StyledSignin = styled.div`
    .Form {
        max-width: 500px;
        width: 50vw;
        min-width: ${layout6xl};
    }
`;
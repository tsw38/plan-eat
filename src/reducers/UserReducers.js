import {combineReducers} from 'redux';

const account = (state = {}, action) => {
    switch (action.type) {
        default:
            return state
    }
}

export default combineReducers({
    account
});